<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dokuapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SiparisAyrinti" />
        <activity android:name=".Urun.UrunAyrinti" />
        <activity android:name=".DokuAnasayfaActivity" />
        <activity android:name=".Login.KaydolActivity" />
        <activity android:name=".Login.GirisYapActivity" />
        <activity android:name=".Login.BaslangicActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>